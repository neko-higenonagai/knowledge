# HGNN 環境セットアップ手順書

このドキュメントでは、`HGNN_setup.bat` を使用してシステムの実行環境（Python および依存ライブラリ）を構築する手順を説明します。

---

## 概要

`HGNN_setup.bat` は以下の作業を自動的に行います：

1. **WinPython のダウンロード**: 軽量でポータブルな Python 3.12 環境を取得します。
2. **展開と配置**: ダウンロードした ZIP を展開し、実行に必要なファイルを `python/` フォルダにコピーします。
3. **ライブラリのインストール**: `requirements.txt` に記載された依存ライブラリをインストールします。
4. **ndlocr-lite のセットアップ**: 国立国会図書館の OCR ライブラリをインストールし、利用可能な状態にします。
5. **作業フォルダの作成**: アプリケーションが使用するフォルダを自動作成します。

---

## 事前準備

- **インターネット接続**: WinPython やライブラリをダウンロードするために必要です。
- **Git のインストール**: `ndlocr-lite` の取得に Git を使用します。コマンドプロンプトで `git --version` が動作することを確認してください。
- **ディスク空容量**: セットアップ完了までに数 GB の空き容量を推奨します。

---

## 実行手順

1. **ファイルの配置**: `HGNN_setup.bat` と `requirements.txt` が同じフォルダにあることを確認してください。
2. **スクリプトの実行**: `HGNN_setup.bat` をダブルクリックするか、コマンドプロンプトから実行します。
3. **完了の確認**: 画面に「Setup Finished Successfully!」と表示されれば完了です。

---

## 注意事項

- `python/` フォルダがすでに存在する場合、WinPython のダウンロードと展開はスキップされ、ライブラリのインストールのみが行われます。
- `Winpython64-3.12.10.1dot.zip` がすでに存在する場合、ダウンロードをスキップして展開処理から開始します。
- `ndlocr-lite` がすでにインストール済みの場合、インストール処理はスキップされます。
- インストール中にエラーが発生した場合は、エラーメッセージを確認し、インターネット接続や Git の設定を見直してください。
- このセットアップはシステム全体を汚さない「ポータブル環境」を作成します。アンインストールしたい場合は、このフォルダを削除するだけで済みます。

---

## 完了後のフォルダ構成

プログラム本体は `scripts/` フォルダに格納されており、各アプリケーションは `run_ragchat.bat` などの起動用 bat ファイルから呼び出します。
`HGNN_setup.bat` はその実行環境（Python・ライブラリ）と作業用フォルダを整備するためのスクリプトです。

セットアップが完了すると、フォルダ構成は以下のようになります：

```
プロジェクトフォルダ/
├── HGNN_setup.bat          # セットアップスクリプト（本スクリプト）
├── requirements.txt        # 依存ライブラリ一覧
├── download_model.bat      # LLMモデルのダウンロード用
├── run_ragchat.bat          # RAGチャット起動用
├── run_*.bat                # その他アプリケーション起動用
├── docs/                   # ドキュメント
├── python/                 # Python 本体とライブラリ（セットアップ時に自動生成）
├── models/                 # AI モデル格納フォルダ（セットアップ時に自動生成）
├── db/                     # データベース格納フォルダ（セットアップ時に自動生成）
├── kb/                     # ナレッジベース格納フォルダ（セットアップ時に自動生成）
└── scripts/                # プログラム本体（変更不要）
```

> **作業用フォルダについて**
> `models/`・`db/`・`kb/` の 3 フォルダはセットアップ時に自動作成されます。
> すでに存在する場合は上書きされないため、再セットアップ時にデータが失われることはありません。
