# HGNN 環境セットアップ手順書 (v0.0.2)

このドキュメントでは、`HGNN_setup.bat` を使用してシステム（HGNN v0.0.2）の実行環境（Python および依存ライブラリ）を構築する手順を説明します。

---

## 概要

`HGNN_setup.bat` は以下の作業を自動的に行います：

1. **WinPython のダウンロード**: 軽量でポータブルな Python 3.12 環境をダウンロードします。
2. **WinPython の展開**: ダウンロードした ZIP ファイルを展開します。
3. **Python バイナリのコピー**: 実行に必要な Python バイナリを `python/` フォルダにコピーします。
4. **一時ファイルのクリーンアップ**: 不要になったダウンロード ZIP や展開先の一時フォルダをクリーンアップします。
5. **ハードウェア構成の選択と基本パッケージのインストール**: 実行環境（CPU / NVIDIA GPU / Intel・AMD GPU）を選択し、共通の依存ライブラリをインストールしてハードウェア設定を保存します（文字認識やベクトル埋め込みは常にCPUで動作し、GPUは対話AIのみを高速化します）。
6. **ONNX Runtime (CPU) のインストール**: 文字認識（NDLOCR-Lite）および埋め込み（Embedding）用として、最も安定したCPU版 ONNX Runtime をインストールします。
7. **llama-cpp-python のコンパイルとインストール**: CPU・GPUを問わず、すべての環境に合わせた高速動作のためのコンパイルおよび再インストールを行います。
8. **ndlocr-lite のセットアップ**: 国立国会図書館の OCR ライブラリ（`ndlocr-lite`）をインストールし、利用可能な状態にします。
9. **作業フォルダの作成と動作確認**: 動作に必要なフォルダ（models/db/kb）を作成し、最終的な動作チェック（OCR コマンドの呼び出し確認）を行います。

---

## 事前準備

> [!IMPORTANT]
> **セットアップを成功させるための必須条件**
> - **Visual Studio C++ Build Tools**:
>   CPU・GPU構成を問わず、対話AIエンジン `llama-cpp-python` をお使いのPC環境でコンパイル・ビルドするために**必須**となります。
>   事前に [Visual Studio C++ Build Tools 公式サイト](https://visualstudio.microsoft.com/ja/visual-cpp-build-tools/) からインストーラーをダウンロードし、ワークロードの **「C++ によるデスクトップ開発」** にチェックを入れてインストールを完了させておいてください。
> - **CUDA Toolkit (NVIDIA GPU を選択する場合のみ)**:
>   NVIDIA GPU 構成を選択してLLMの推論を高速化させる場合は、事前にシステムに **CUDA Toolkit** (12.x 推奨) がインストールされている必要があります。

---

## ハードウェア構成（CPU / GPU）に関する重要な注意点

> [!TIP]
> **推奨構成：CPU環境 [最も安定しています]**
> - **推奨環境**: 本システムは、最も安定して動作する **「CPU環境」を推奨構成（動作保証）** としています。特段の理由がない限り、セットアップ時は `[1] CPU のみ` の選択を強くおすすめします。
> - **文字認識および埋め込みのCPU固定について**:
>   文字認識（NDLOCR-Lite）およびベクトル埋め込み（Embedding: `harrier-oss-v1-270m`）は、一部の GPU ドライバや ONNX Execution Provider (CUDA EP等) との互換性による異常メモリ確保バグやクラッシュを防ぐため、**常に安定した CPU 上で動作するよう設計（CPU固定）** されています。これらのモデルは非常に軽量であるため、CPU 上でも数ミリ秒〜数十ミリ秒で超高速に動作し、全体のパフォーマンスに影響はありません。
> - **GPUによる高速化範囲**: GPU構成（NVIDIA または Intel/AMD）を選択した際に高速化されるのは、**対話AI（LLM）の推論のみ**となります。

---

## セットアップ手順

1. **バッチファイルの実行**:
   プロジェクトフォルダ直下にある `HGNN_setup.bat` をダブルクリック（またはコマンドプロンプトから実行）します。
2. **事前準備の確認**:
   「【重要】事前準備の確認」プロンプトが表示されます。C++ Build Tools のインストールを確認し、**Enter キー**を押して続行します。
3. **ハードウェア構成の選択**:
   コンソール上に選択肢が表示されます。お使いの環境に合わせて選択し、番号を入力して Enter キーを押します：
   - `[1] CPU のみ [推奨・最も安定しています]`
   - `[2] NVIDIA GPU [CUDA アクセラレーション - RTX 2060等検証済み]`
   - `[3] Intel / AMD GPU [DirectML / Vulkan アクセラレーション - 未検証・サポート外]`
4. **GPUインストールの警告確認 (GPU選択時のみ)**:
   GPU（NVIDIA または Intel/AMD）を選択した場合、未検証または C++ ビルド環境要件に関する警告が表示されます。内容を確認し、問題なければ `Y` キーを入力して Enter を押します。
5. **インストールプロセスの完了を待つ**:
   llama-cpp-python のコンパイルおよびインストールが行われます。
   ※GPU選択時はコンパイルに **10分〜30分程度** かかる場合があります。フリーズしているように見えても、バックグラウンドでビルドが走っていますので、ウィンドウを閉じずにそのままお待ちください。
6. **セットアップの完了**:
   「セットアップが正常に完了しました！」と表示されたら、任意のキーを押して終了します。

---

## 完了後のフォルダ構成

セットアップが完了すると、フォルダ構成は以下のようになります：

```
プロジェクトフォルダ/
├── HGNN_setup.bat          # セットアップスクリプト（本スクリプト）
├── requirements.txt        # 依存ライブラリ一覧
├── download_model.bat      # LLMモデルのダウンロード用
├── run_ragchat.bat          # RAGチャット起動用
├── run_*.bat                # その他アプリケーション起動用
├── docs/                   # ドキュメント
├── python/                 # Python 本体とライブラリ（セットアップ時に自動生成）
├── models/                 # AI モデル格納フォルダ（セットアップ時に自動生成）
├── db/                     # データベース格納フォルダ（セットアップ時に自動生成）
├── kb/                     # ナレッジベース格納フォルダ（セットアップ時に自動生成）
└── scripts/                # プログラム本体（変更不要）
```

> **作業用フォルダについて**
> `models/`・`db/`・`kb/` の 3 フォルダはセットアップ時に自動作成されます。
> すでに存在する場合は上書きされないため、再セットアップ時にデータが失われることはありません。
